{
  "name": "@optimizely/sdk-plugin-pending-events",
  "version": "0.2.0",
  "description": "Event Dispatcher plugin that provides a persistent pending event queue",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "prepare": "npm run build",
    "doc": "documentation readme src/index.js -s API --markdown-toc false --github",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "documentation": "^6.3.2",
    "prettier": "^1.12.1"
  },
  "directories": {
    "lib": "lib"
  },
  "dependencies": {}
}
